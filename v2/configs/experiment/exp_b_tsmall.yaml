# @package _global_
# ==============================================================================
# 實驗配置：Exp B T_small (啟用前處理，無增強，BatchSize=32，訓練集=T_small)
# ==============================================================================

data:
  use_connected_components: true
  use_topology_analysis: true
  use_topology_pruning: true
  remove_gifu_logo: true
  skip_preprocessing: false
  dataset_dir: "dataset_v2/T_small"

training:
  use_preprocessing: true
  use_augmentation: false
  batch_size: 32

experiment:
  exp_name: "exp_b_tsmall"
